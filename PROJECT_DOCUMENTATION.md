# مستند پروژه (ZBackend)

این مخزن یک پروژه بک‌اند نوشته‌شده با NestJS و Prisma است که برای فروشگاه/مارکت‌پلیس محلی طراحی شده است. در این مستند راه‌اندازی، ساختار کلی پروژه و نکات مهم برای توسعه و اجرای پروژه آمده است.

## تکنولوژی‌ها

- Node.js (NestJS)
- TypeScript
- Prisma (MySQL)
- Jest (برای تست‌ها)

## پیش‌نیازها

- Node.js >= 18
- npm یا pnpm/yarn
- یک دیتابیس MySQL در دسترس

## راه‌اندازی اولیه

1. مخزن را کلون کنید و وارد پوشه پروژه شوید.
2. وابستگی‌ها را نصب کنید:

```bash
npm install
# یا
# pnpm install
```

3. یک نسخه از فایل `env.example` بسازید و مقدار متغیرهای محیطی لازم را تنظیم کنید (`.env`).

4. Prisma را پیکربندی و client را تولید کنید:

```bash
npx prisma generate
npx prisma migrate deploy   # برای محیط تولید
# یا در محیط توسعه برای اجرای مایگریشن‌ها و ساختن دیتابیس محلی
npx prisma migrate dev
```

5. (اختیاری) داده‌های اولیه را seed کنید:

```bash
node --loader ts-node/esm seed.ts
```

## اسکریپت‌های مفید

- `npm run start:dev` — اجرای برنامه در حالت توسعه
- `npm run build` — بیلد TypeScript
- `npm run start:prod` — اجرای نسخه تولیدی
- `npm run test` — اجرای تست‌ها (Jest)

## ساختار کلی پروژه (کلیدی)

- `/src` — کد منبع برنامه
  - `auth/` — احراز هویت، گاردها و DTOها
  - `users/` — مدیریت کاربران
  - `products/` — مدیریت محصولات
  - `business/` — مدیریت کسب‌وکارها (فروشندگان)
  - `categories/` — دسته‌بندی محصولات
  - `orders/` — سفارشات و آیتم‌های سفارش
  - `upload/` — آپلود فایل‌ها/تصاویر
  - `prisma/` — سرویس Prisma برای دسترسی به دیتابیس

## فایل‌های مهم

- [prisma/schema.prisma](prisma/schema.prisma#L1-L288) — مدل‌های دیتابیس و اسکیما
- `env.example` — متغیرهای محیطی پیشنهادی
- `seed.ts` — اسکریپت seed (در صورت وجود داده‌های اولیه)

## نکات دیتابیس و Prisma

- اسکیما در `prisma/schema.prisma` تعریف شده و از MySQL استفاده می‌کند.
- پس از تغییر اسکیما، اجرا کنید:

```bash
npx prisma migrate dev --name your_migration_name
npx prisma generate
```

## آشنایی با API و ماژول‌ها

ماژول‌های اصلی در `src` قرار دارند و هر ماژول معمولاً شامل controller، service و DTOها است. برخی از مسیرها/قابلیت‌های مهم:

- احراز هویت: `src/auth` (ورود، ثبت‌نام، OTP اگر فعال باشد)
- کاربران: `src/users`
- محصولات: `src/products` و `src/product-image`, `src/product-image/`
- کسب‌وکارها: `src/business` و `src/business-image`
- سفارشات: `src/orders` و `src/order-items` (در صورتی که جدا شده باشد)
- دسته‌بندی‌ها: `src/categories`
- بارگذاری فایل: `src/upload`

برای مشاهده مسیرهای دقیق کنترلرها، فایل‌های `*controller.ts` را در `src` بررسی کنید.

## تست

اجرای تست‌ها با Jest:

```bash
npm run test
```

## توسعه و مشارکت

- قبل از ارسال Pull Request، لطفاً تست‌های مرتبط را اجرا کنید و کد را بیلد کنید.
- قوانین lint و فرمتینگ را در صورت تنظیم رعایت کنید.

## تماس و پشتیبانی

در صورت سؤال یا نیاز به دسترسی به سرویس‌ها، جزئیات را در Issues مخزن ثبت کنید یا مستقیماً با نگهدارنده پروژه تماس بگیرید.

---

اگر می‌خواهید این مستند انگلیسی شود، نسخه خلاصه‌تری بسازم یا مستندات API کامل‌تر (مسیرها، بدنه درخواست/پاسخ) اضافه کنم بگو تا انجام دهم.
